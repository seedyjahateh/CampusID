"""Authorization: roles, entitlements and the decision engine (FR-AZ-01 to 08).

Authentication answers who somebody is. This answers what they may do, which is
a separate question with a separate failure mode: an authentication bug lets the
wrong person in, and an authorization bug lets the right person do the wrong
thing.
"""
