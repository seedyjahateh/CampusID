"""The downstream OpenID Connect provider.

The broker is a SAML SP upstream and an OIDC OP downstream: applications speak
OIDC to us, we speak SAML to the campus IdP. That bridge is the point of the
whole system.
"""
